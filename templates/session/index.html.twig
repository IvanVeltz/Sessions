{% extends 'base.html.twig' %}

{% block title %}Liste des sessions {% endblock %}

{% block body %}
    <h1>Liste des sessions pour {{sessions[0].training.name}}</h1>

    {% for session in sessions %}
        {{session.name}} <br>
    {% endfor %}

{% endblock %}
